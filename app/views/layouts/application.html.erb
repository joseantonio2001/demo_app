<!DOCTYPE html>
<html>
<head>
  <title>DemoApp</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body>
  <nav style="background: #eee; padding: 10px; margin-bottom: 20px;">
    <strong>Navegación:</strong>
    <%= link_to "Página Principal", root_path %> |
    <%= link_to "Posts", posts_path %> |  <%# <-- ENLACE AÑADIDO AQUÍ %>

    <% if user_signed_in? %>
      <%= link_to "Dashboard (Protegido)", dashboard_path %> |
      <span>Hola, <%= current_user.email %>!</span>
      <%= button_to "Cerrar Sesión", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "Iniciar Sesión", new_user_session_path %> |
      <%= link_to "Registrarse", new_user_registration_path %>
    <% end %>
  </nav>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= yield %>
</body>
</html>